# Requires Python >=3.11 (band-sdk constraint). Use uv: `uv pip install -r requirements.txt`.

# --- Band multi-agent SDK (drives langchain/langgraph/langchain-openai) ---
band-sdk[langgraph]==1.0.0
langchain-openai>=1.3.0          # ChatOpenAI pointed at AI/ML API + Featherless (OpenAI-compatible)

# --- Web framework ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sse-starlette>=2.1.3
python-multipart>=0.0.20

# --- Config / validation ---
pydantic>=2.10.4
pydantic-settings>=2.7.1
python-dotenv>=1.0.1

# --- Database (async PostgreSQL + pgvector) ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
pgvector>=0.3.6
greenlet>=3.1.1

# --- Embeddings / RAG ---
sentence-transformers>=3.3.1

# --- HTTP client ---
httpx>=0.28.1
